import { PinnedNewsfeedPipe } from './pinned-newsfeed.pipe';

describe('PinnedNewsfeedPipe', () => {
  it('create an instance', () => {
    const pipe = new PinnedNewsfeedPipe();
    expect(pipe).toBeTruthy();
  });
});
